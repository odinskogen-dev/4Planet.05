{
  "name": "@4planet/production-factory",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types && node scripts/patch-agent-types.mjs",
    "typecheck": "npm run types && tsc --noEmit",
    "test": "tsx --test src/*.test.ts"
  },
  "dependencies": {
    "agents": "^0.17.0"
  },
  "devDependencies": {
    "tsx": "^4.20.0",
    "typescript": "^5.5.4",
    "wrangler": "^4.0.0"
  }
}
