name: 4PLANET Magazine legacy live deploy — DISABLED

# P0 release-control repair — 2026-09-08
#
# This historical recovery/sandbox workflow previously deployed directly to
# 4planetmagazine.com on every push to recovery/testking-magazine. That violates
# the current FOUR STATE law: SANDBOX has zero LIVE write authority and all LIVE
# mutation is Founder-gated exact-artifact promotion only.
#
# Keep the file as historical control evidence, but make it deliberately inert.
# Production promotion must use the canonical Founder-gated release path and an
# explicitly approved immutable artifact.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked-legacy-path:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed — legacy sandbox live deploy is disabled
        shell: bash
        run: |
          echo 'BLOCKED: recovery/testking-magazine has zero LIVE write authority.'
          echo 'Use canonical Founder-gated exact-artifact promotion after explicit release approval.'
          exit 1
