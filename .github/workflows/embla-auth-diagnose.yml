name: 4SAPIEN source diagnose

on:
  push:
    branches: [build/ask-embla-live-20260909]
    paths: ['.github/workflows/embla-auth-diagnose.yml']
  workflow_dispatch:

permissions:
  contents: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Materialize source and inspect Yellow 02 anchors
        run: |
          set -euo pipefail
          cat products/4sapien/source/part-{00..06}.b64 | base64 -d | xz -d > /tmp/embla.html
          python - <<'PY'
          import pathlib,re
          s=pathlib.Path('/tmp/embla.html').read_text()
          terms=['function ProductRow','function Detail','function ProfileFields','function Meals','function Economy','function Learn','function SearchBox','function Search','function Handle','function Meg']
          for term in terms:
              print('\n===',term,'===')
              m=re.search(re.escape(term),s)
              if m:
                  a=max(0,m.start()-250); b=min(len(s),m.start()+5200)
                  print(s[a:b])
          PY
