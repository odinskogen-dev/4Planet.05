name: 4PLANET Films legacy live founder override — DISABLED

# P0 release-control hardening — 2026-09-08
#
# This workflow was used for the Founder-authorised emergency recovery that
# restored the correct 4PLANET Magazine + Films + dark-mode artifact to LIVE.
# It previously triggered production mutation on ordinary pushes to
# work/magazine-public-launch-gold-01. That is not compatible with current
# FOUR STATE law: work/sandbox branches have zero standing LIVE authority.
#
# Keep this workflow as historical recovery evidence only. Future Magazine LIVE
# promotion must go through the canonical Founder-gated exact-artifact release
# path, with current LIVE archived first and post-promotion runtime readback.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  blocked-legacy-path:
    runs-on: ubuntu-latest
    steps:
      - name: Fail closed — no standing Magazine LIVE authority
        shell: bash
        run: |
          echo 'BLOCKED: this historical work branch has zero standing LIVE write authority.'
          echo 'Use canonical exact-artifact promotion only after action-specific Founder release approval.'
          exit 1
