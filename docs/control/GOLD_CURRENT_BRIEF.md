# CURRENT GOLD BRIEF

This file is the machine-readable human contract for the **current bounded TEST KING change**. Historical briefs belong in issue/PR evidence; this file always reflects the current mutation.

**CHANGE ID:** TEST-KING-ATLAS-RETURN-CAMERA-58  
**STATUS:** TEST / FOUNDER-DIRECTED BUILD + QA  
**BASE AUTHORITY:** `king/test` / PR #131 / issue #133 / ORCA + JAGUAR GOLD CONTINUOUS CONVERGENCE ORDER  
**FOUNDER DIRECTION:** keep coding until the Jaguar/Orca convergence line is exact-head green. The dedicated Jaguar full-source browser proof is now passing; correct remaining shared TEST KING failures without weakening product truth or reverting accepted recovered surfaces.  
**SINGLE-SEAM RULE:** `king/test` is the only moving integration line. Historical Jaguar/Orca branches are donors only. LIVE KING/production remains untouched.

## USER ARRIVES BECAUSE
They move from ATLAS into a species or Journey to understand more, then expect RETURN TO ATLAS to restore the exact world context they left.

## ONE THING TO UNDERSTAND
A preserved return state owns the camera. Reopening the subject may restore the context panel and data, but it must not silently refocus or overwrite the saved world view.

## PRIMARY ACTION
Move/zoom ATLAS, enter a linked species/Journey, then return to the same camera and record context.

## SECONDARY DEPTH
Continue from the restored ATLAS state into the same species, occurrence, Living System and Journey relationships without losing orientation.

## P1 DOMINANT
Exact restored ATLAS camera on desktop and mobile.

## P2 ORIENTATION
The same record context is available after return without forcing a second camera move.

## P3 ACTION / NEXT
Pan/zoom, open context, move into a deeper surface and RETURN TO ATLAS with preserved state.

## P4 DEPTH
Source/provenance, layers, lenses and relationship context remain available after reconstruction.

## CURRENT DEFECT
Exact Convergence #1862 on `79666302579540ec0b3470421471307662a1e3ba` passes policy, typecheck, production build, 89/89 smoke/contracts, lint, assets and dependency gates, and 15/16 shared Chromium vertical-slice/switcher tests. The only failure is mobile-430 return-state reconstruction: restored live zoom differs from the saved post-interaction zoom by 1.33, above the 0.25 contract. Desktop, 1280 and mobile-390 pass. The same exact head's dedicated Jaguar P0 full-source Chromium/WebKit desktop/mobile step passes, so this blocker is ATLAS camera ownership, not Jaguar rendering.

The saved ATLAS state contains explicit `z` + `c` camera coordinates together with `record=orca-bundled` and a transient `entity` value written from the open observation's taxon. On reconstruction ATLAS first executes the generic entity deep-link path. That path resolves the taxon asynchronously and calls `fitRows()`, allowing a second camera fit to overwrite the saved world view. The record itself already reconstructs the exact bundled Orca observation/context and exposes the Species handoff, so the simultaneous entity focus is redundant during a camera-owning record return.

## BOUNDED ADOPTION
Canonicalise ProductContext at the return boundary: when a preserved state contains explicit `z` + `c` plus `record`, keep the camera, record, layers, mode, projection, lens and journey state, but omit the redundant transient `entity` refocus from the returned href. Direct entity-only deep links and return states without a record keep `entity` unchanged. The `entity` key remains part of the active schema. Do not loosen the E2E tolerance, remove record truth or change normal direct-entity behaviour.

## WHAT CAN BE REMOVED
Only the redundant return-time `entity` focus when an explicit camera and record already reconstruct the same context; duplicate boot-time camera ownership; test-only workarounds that merely increase tolerance.

## WHAT MUST BE REUSED
`king/test`, PR131, existing ProductContext/returnTo schema, existing ATLAS URL camera (`z`,`c`), record state, one shared Context Layer, current Journey return controls, current Jaguar V52 full-source path and all already-green Orca/Jaguar contracts.

## TRUTH BOUNDARY
This change affects navigation/camera reconstruction only. The canonical bundled record remains present and reconstructs the same observation/source truth. The schema still supports `entity`; only a redundant entity refocus is normalised out when record + explicit camera own reconstruction. No biological occurrence, species, route, migration, abundance, live-data, Ear.Rodriguez licence, geometry, material or animation claim changes. `PILOT CORRIDOR ≠ MIGRATION TRACK` remains locked. LIVE KING remains untouched.

## PERFORMANCE
Avoid a second async occurrence-driven camera animation on restored return. Reusing the saved camera is both more deterministic and cheaper on mobile. No new network source, renderer or observer is introduced.

## MOBILE-FIRST RISK
390 and 430 must reconstruct the same saved zoom/centre without viewport-specific race behaviour, retain tappable controls and record context, and avoid layout/animation side effects.

## HUMAN SUCCESS
A user can zoom and move the world, enter the Orca/species/Journey path, RETURN TO ATLAS and land in the same world view they left, with the relevant bundled observation/context still available.

## ACCEPTANCE
1) Typecheck + production build + smoke/contracts green. 2) ProductContext contracts prove camera+record return canonicalisation while direct entity-only return still round-trips. 3) Gate 1 vertical slice + product switcher pass Chromium desktop-1440, desktop-1280, mobile-390 and mobile-430 with camera delta <= 0.25. 4) Jaguar dedicated full-source Chromium/WebKit desktop + 390/430 proof remains green. 5) Remaining shared Browser Product Proof and Convergence matrices complete green. 6) Exact SHA deployment verified before ACCEPTED/SAFE. 7) LIVE KING unchanged.
